<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dayan.food.mapper.FoodMapper">

    <resultMap id="foodResultMap" type="com.dayan.food.entity.po.Food">
        <id property="id" column="food_id"/>
        <result property="name" column="food_name"/>
        <result property="latitude" column="latitude"/>
        <result property="longitude" column="longitude"/>
        <result property="address" column="address"/>
        <result property="summary" column="summary"/>
        <result property="story" column="story"/>
        <result property="ingredients" column="ingredients"/>
        <result property="imageUrl" column="image_url"/>
        <result property="remark" column="remark"/>
        <result property="heat" column="heat"/>
        <result property="reviewStatus" column="review_status" javaType="com.dayan.food.entity.enums.FoodReviewStatus"/>
        <result property="reviewedBy" column="reviewed_by"/>
        <result property="reviewedAt" column="reviewed_at"/>
        <result property="createdBy" column="created_by"/>
        <result property="createdAt" column="created_at"/>
        <association property="region" javaType="com.dayan.food.entity.po.Region">
            <id property="id" column="region_id"/>
            <result property="name" column="region_name"/>
            <result property="province" column="region_province"/>
            <result property="description" column="region_description"/>
        </association>
    </resultMap>

    <resultMap id="foodFootprintResultMap" type="com.dayan.food.entity.po.FoodFootprint">
        <result property="visitedAt" column="footprint_visited_at"/>
        <association property="food" resultMap="foodResultMap"/>
    </resultMap>

    <resultMap id="markerResultMap" type="com.dayan.food.entity.po.FoodMarker">
        <id property="id" column="food_id"/>
        <result property="name" column="food_name"/>
        <result property="latitude" column="latitude"/>
        <result property="longitude" column="longitude"/>
        <result property="summary" column="summary"/>
        <association property="region" javaType="com.dayan.food.entity.po.Region">
            <id property="id" column="region_id"/>
            <result property="name" column="region_name"/>
            <result property="province" column="region_province"/>
        </association>
    </resultMap>

    <sql id="foodColumns">
        f.id AS food_id,
        f.name AS food_name,
        f.latitude,
        f.longitude,
        f.address,
        f.summary,
        f.story,
        f.ingredients,
        f.image_url,
        f.remark,
        f.heat,
        f.review_status,
        f.reviewed_by,
        f.reviewed_at,
        f.created_by,
        f.created_at,
        r.id AS region_id,
        COALESCE(NULLIF(f.city, ''), r.name) AS region_name,
        COALESCE(NULLIF(f.province, ''), r.province) AS region_province,
        r.description AS region_description
    </sql>

    <!-- 前台列表/地图标记/目录分页共用的已审核过滤条件（仅引用 food 表列，count 可免 join 复用）。 -->
    <sql id="listConditions">
        f.review_status = 'APPROVED'
        <if test="keyword != null and keyword != ''">
            AND LOWER(f.name) LIKE CONCAT('%', LOWER(#{keyword}), '%')
        </if>
        <if test="regionId != null">
            AND f.region_id = #{regionId}
        </if>
        <!-- 关键词搜索是全局结果：命中即返回，不随地图可视范围过滤；浏览模式才按视口收窄。 -->
        <if test="(keyword == null or keyword == '') and minLatitude != null and maxLatitude != null and minLongitude != null and maxLongitude != null">
            AND f.latitude BETWEEN #{minLatitude} AND #{maxLatitude}
            <choose>
                <when test="minLongitude &lt;= maxLongitude">
                    AND f.longitude BETWEEN #{minLongitude} AND #{maxLongitude}
                </when>
                <otherwise>
                    AND (f.longitude &gt;= #{minLongitude} OR f.longitude &lt;= #{maxLongitude})
                </otherwise>
            </choose>
        </if>
    </sql>

    <select id="findList" resultMap="foodResultMap">
        SELECT <include refid="foodColumns"/>
        FROM food f
        LEFT JOIN region r ON r.id = f.region_id
        <where>
            <include refid="listConditions"/>
        </where>
        ORDER BY f.heat DESC, f.id DESC
        LIMIT #{limit}
    </select>

    <select id="findMarkers" resultMap="markerResultMap">
        SELECT
            f.id AS food_id,
            f.name AS food_name,
            f.latitude,
            f.longitude,
            f.summary,
            r.id AS region_id,
            COALESCE(NULLIF(f.city, ''), r.name) AS region_name,
            COALESCE(NULLIF(f.province, ''), r.province) AS region_province
        FROM food f
        LEFT JOIN region r ON r.id = f.region_id
        <where>
            <include refid="listConditions"/>
        </where>
        ORDER BY f.heat DESC, f.id DESC
        LIMIT #{limit}
    </select>

    <select id="findCatalogPage" resultMap="foodResultMap">
        SELECT <include refid="foodColumns"/>
        FROM food f
        LEFT JOIN region r ON r.id = f.region_id
        <where>
            <include refid="listConditions"/>
        </where>
        ORDER BY f.heat DESC, f.id DESC
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <select id="countCatalog" resultType="int">
        SELECT COUNT(*)
        FROM food f
        <where>
            <include refid="listConditions"/>
        </where>
    </select>

    <select id="findAdminPage" resultMap="foodResultMap">
        SELECT <include refid="foodColumns"/>
        FROM food f
        LEFT JOIN region r ON r.id = f.region_id
        <if test="status != null">
            WHERE f.review_status = #{status}
        </if>
        ORDER BY FIELD(f.review_status, 'PENDING', 'APPROVED', 'REJECTED'), f.id DESC
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <select id="findByCreatedBy" resultMap="foodResultMap">
        SELECT <include refid="foodColumns"/>
        FROM food f
        LEFT JOIN region r ON r.id = f.region_id
        WHERE f.created_by = #{username}
        ORDER BY f.created_at DESC, f.id DESC
    </select>

    <select id="findApprovedByCreatedBy" resultMap="foodResultMap">
        SELECT <include refid="foodColumns"/>
        FROM food f
        LEFT JOIN region r ON r.id = f.region_id
        WHERE f.review_status = 'APPROVED'
          AND f.created_by = #{username}
        ORDER BY f.heat DESC, f.id DESC
        LIMIT #{limit}
    </select>

    <select id="findApprovedForMatching" resultMap="foodResultMap">
        SELECT <include refid="foodColumns"/>
        FROM food f
        LEFT JOIN region r ON r.id = f.region_id
        WHERE f.review_status = 'APPROVED'
        ORDER BY f.id DESC
    </select>

    <select id="findOwnedById" resultMap="foodResultMap">
        SELECT <include refid="foodColumns"/>
        FROM food f
        LEFT JOIN region r ON r.id = f.region_id
        WHERE f.id = #{id}
          AND f.created_by = #{username}
    </select>

    <update id="updateOwnedDetails">
        UPDATE food
        SET name = #{name},
            region_id = #{regionId},
            latitude = #{latitude},
            longitude = #{longitude},
            address = #{address},
            summary = #{summary},
            story = #{story},
            ingredients = #{ingredients},
            image_url = #{imageUrl},
            remark = #{remark},
            review_status = #{status},
            reviewed_by = #{reviewedBy},
            reviewed_at = CASE WHEN #{status} = 'APPROVED' THEN CURRENT_TIMESTAMP ELSE NULL END
        WHERE id = #{id}
          AND created_by = #{username}
    </update>

    <select id="countAdmin" resultType="int">
        SELECT COUNT(*)
        FROM food
        <if test="status != null">
            WHERE review_status = #{status}
        </if>
    </select>

    <select id="sumHeat" resultType="long">
        SELECT COALESCE(SUM(heat), 0)
        FROM food
    </select>

    <select id="countPending" resultType="int">
        SELECT COUNT(*)
        FROM food
        WHERE review_status = 'PENDING'
    </select>

    <select id="countByImageUrl" resultType="int">
        SELECT COUNT(*)
        FROM food
        WHERE image_url = #{imageUrl}
    </select>

    <select id="findById" resultMap="foodResultMap">
        SELECT <include refid="foodColumns"/>
        FROM food f
        LEFT JOIN region r ON r.id = f.region_id
        WHERE f.id = #{id}
          AND f.review_status = 'APPROVED'
    </select>

    <update id="updateLocationLabels">
        UPDATE food SET province = #{province}, city = #{city} WHERE id = #{id}
    </update>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO food (
            name, region_id, latitude, longitude, address, summary, story,
            ingredients, image_url, remark, heat, review_status, reviewed_by, reviewed_at, created_by, created_at
        )
        VALUES (
            #{name}, #{region.id}, #{latitude}, #{longitude}, #{address}, #{summary}, #{story},
            #{ingredients}, #{imageUrl}, #{remark}, #{heat}, #{reviewStatus}, #{reviewedBy}, #{reviewedAt},
            #{createdBy}, #{createdAt}
        )
    </insert>

    <select id="countDuplicate" resultType="int">
        SELECT COUNT(*)
        FROM food
        WHERE name = #{name}
          AND region_id = #{regionId}
          AND address &lt;=&gt; #{address}
    </select>

    <insert id="insertDailyVisit">
        INSERT IGNORE INTO food_daily_visit (food_id, user_id, visit_date, visited_at)
        SELECT f.id, u.id, CURRENT_DATE, CURRENT_TIMESTAMP
        FROM food f
        INNER JOIN app_user u ON u.username = #{username} AND u.active = TRUE
        WHERE f.id = #{foodId}
          AND f.review_status = 'APPROVED'
    </insert>

    <update id="touchDailyVisit">
        UPDATE food_daily_visit v
        INNER JOIN app_user u ON u.id = v.user_id
        SET v.visited_at = CURRENT_TIMESTAMP
        WHERE v.food_id = #{foodId}
          AND u.username = #{username}
          AND v.visit_date = CURRENT_DATE
    </update>

    <select id="findRecentVisits" resultMap="foodFootprintResultMap">
        SELECT <include refid="foodColumns"/>,
               v.visited_at AS footprint_visited_at
        FROM food_daily_visit v
        INNER JOIN app_user u ON u.id = v.user_id
        INNER JOIN food f ON f.id = v.food_id
        LEFT JOIN region r ON r.id = f.region_id
        WHERE u.username = #{username}
          AND f.review_status = 'APPROVED'
        ORDER BY v.visited_at DESC, f.id DESC
        LIMIT #{limit}
    </select>

    <select id="findAgentRecommendations" resultMap="foodResultMap">
        SELECT <include refid="foodColumns"/>
        FROM food f
        LEFT JOIN region r ON r.id = f.region_id
        WHERE f.review_status = 'APPROVED'
        <if test="province != null and province != ''">
            AND r.province LIKE CONCAT('%', #{province}, '%')
        </if>
        <if test="city != null and city != ''">
            AND r.name LIKE CONCAT('%', #{city}, '%')
        </if>
        <if test="personalized and username != null and username != ''">
            AND NOT EXISTS (
                SELECT 1
                FROM food_daily_visit seen
                INNER JOIN app_user seen_user ON seen_user.id = seen.user_id
                WHERE seen.food_id = f.id
                  AND seen_user.username = #{username}
                  AND seen.visited_at &gt;= CURRENT_TIMESTAMP - INTERVAL 30 DAY
            )
        </if>
        ORDER BY
        <if test="personalized and username != null and username != ''">
            (
                SELECT COUNT(*)
                FROM food_daily_visit preference
                INNER JOIN app_user preference_user ON preference_user.id = preference.user_id
                INNER JOIN food viewed_food ON viewed_food.id = preference.food_id
                WHERE preference_user.username = #{username}
                  AND preference.visited_at &gt;= CURRENT_TIMESTAMP - INTERVAL 30 DAY
                  AND viewed_food.region_id = f.region_id
            ) DESC,
        </if>
        f.heat DESC, f.id DESC
        LIMIT #{limit}
    </select>

    <update id="incrementHeat">
        UPDATE food
        SET heat = heat + 1
        WHERE id = #{id}
    </update>

    <update id="updateReviewStatus">
        UPDATE food
        SET review_status = #{status},
            reviewed_by = #{reviewedBy},
            reviewed_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
          AND review_status = 'PENDING'
    </update>

    <delete id="deleteById">
        DELETE FROM food
        WHERE id = #{id}
    </delete>
</mapper>
